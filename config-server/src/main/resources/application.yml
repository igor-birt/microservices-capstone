server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///Users/ibirt/IdeaProjects/microservicesCapstone/config-repo
          clone-on-start: true

management:
  endpoints:
    web:
      exposure:
        include: health,info